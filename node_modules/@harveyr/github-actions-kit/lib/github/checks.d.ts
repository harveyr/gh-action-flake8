import { CheckRunAnnotation, ChecksCreateParamsOutputAnnotations } from './types';
/**
 * Converts an annotation object from our internal interface to what the Github API expects.
 * @param d
 */
export declare function convertAnnotation(d: CheckRunAnnotation): ChecksCreateParamsOutputAnnotations;
