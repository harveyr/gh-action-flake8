"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
var core = __importStar(require("@actions/core"));
var _a = process.env, GITHUB_REPOSITORY = _a.GITHUB_REPOSITORY, GITHUB_SHA = _a.GITHUB_SHA, GITHUB_WORKSPACE = _a.GITHUB_WORKSPACE;
function getWorkspace() {
    if (!GITHUB_WORKSPACE) {
        throw new Error('GITHUB_WORKSPACE is empty');
    }
    return GITHUB_WORKSPACE;
}
exports.getWorkspace = getWorkspace;
function getOwnerAndRepo(s) {
    if (!s) {
        if (!GITHUB_REPOSITORY) {
            throw new Error('GITHUB_REPOSITORY is empty');
        }
        s = GITHUB_REPOSITORY;
    }
    var _a = s.split('/'), owner = _a[0], repo = _a[1];
    core.debug("Found Github owner " + owner + ", repo " + repo);
    return { owner: owner, repo: repo };
}
exports.getOwnerAndRepo = getOwnerAndRepo;
function getSha() {
    if (!GITHUB_SHA) {
        throw new Error('GITHUB_SHA is empty');
    }
    return GITHUB_SHA;
}
exports.getSha = getSha;
